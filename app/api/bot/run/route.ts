export const dynamic='force-dynamic'
export async function GET(){
  return Response.json({bot:'DRAMA.AI BOT LIVE', status:'OK', scheduled:3, message:'Bot works - add Supabase + FAL_KEY for real AI films'})
}
